h1 Lean Elevator Challenge

- if Round.any?
  h2 Round #{@round_id}

  pre#simulator[data-round=@round_id data-round-max=Round.maximum(:id)] = @output

  ul.list-inline
    li
      strong = link_to '<', stats_simulator_path(round_id: @round_id - 1) if @round_id > Round.minimum(:id)
    li
      strong = link_to '>', stats_simulator_path(round_id: @round_id + 1) if @round_id < Round.maximum(:id)

- else
  p Simulator is not available until the game started.
